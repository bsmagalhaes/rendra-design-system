// Fixture de teste (src/cli/trocar.test.ts): variant={x}, dinâmica, nunca reescrita.
import { Tabs } from '@rendra-ui/web'

export function Pagina({ x }: { x: string }) {
  return <Tabs variant={x}>Conteúdo</Tabs>
}
