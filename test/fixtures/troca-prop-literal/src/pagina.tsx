// Fixture de teste (src/cli/trocar.test.ts): a prop variant abaixo é literal, viável de reescrever.
import { Tabs } from '@rendra-ui/web'

export function Pagina() {
  return <Tabs variant="line">Conteúdo</Tabs>
}
