dist
storybook-static
playwright-report
test-results
screenshots
node_modules
coverage
# Fixtures da CLI: código de exemplo com violação de propósito, nunca
# reformatado (o teste de rendra trocar confere o byte exato do arquivo antes e depois).
test/fixtures
# Verificação do Google Search Console: o conteúdo precisa ficar exatamente como veio
public/google*.html
