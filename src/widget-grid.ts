/*
 * ENTRADA SEPARADA: WidgetGrid (@rendra-ui/web/widget-grid)
 * ------------------------------------------------------------
 * Fora da entrada principal (src/index.ts): depende de `react-grid-layout`, pesado, só
 * entra no bundle de quem realmente usa o dashboard arrastável.
 */
export * from './components/ui/widget-grid'
